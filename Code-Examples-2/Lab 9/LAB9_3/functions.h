#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define A2B(A,B) pow(A,B)
#define INVERSE(A) (double)(1)/(A)
#define MIN(A,B) ((A)<(B)?(A):(B))
#define MAX(A,B) ((A)>(B)?(A):(B))
#define CUBE(A) (A)*(A)*(A)
